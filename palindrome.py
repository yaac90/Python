#!/usr/bin/python3

def palindrome(string_Word):
    """
    It checks if a word is a palindrome.
    
    :param string_Word: The word that will be checked to see if it's a palindrome
    """
    if len(string_word >= 0)
        lower_string = string_Word.lower()
        return  lower_string == lower_string[::-1]
    else
        return "String must to be bigger than 0"
